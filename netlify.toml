# Hosting for web app
[build]
  base = "webapp/"
  command = "REACT_APP_API_STAGE=dev npm run build"
  publish = "webapp/build/"

[context.production]
  command = "REACT_APP_API_STAGE=prod npm run build"

# Always redirect any request to our index.html and return the status code 200.
[[redirects]]
  from    = "/*"
  to      = "/index.html"
  status  = 200