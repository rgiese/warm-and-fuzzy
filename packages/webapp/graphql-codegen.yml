schema: ../shared/src/schema/schema.graphql

generates:
  ./src/generated/graphqlClient.tsx:
    documents: ./src/**/!(graphqlClient).tsx
    config:
      scalars:
        DateTime: Date
    plugins:
      - add: "/* eslint-disable */"
      - typescript
      - typescript-operations
      - typescript-react-apollo
