import { StyleSheet } from "react-native";

const ScreenBaseStyle = StyleSheet.create({
  // Top-level view
  topLevelView: {
    flex: 1,
    flexDirection: "column",
    paddingLeft: 20,
    paddingRight: 20,
  },
});

export default ScreenBaseStyle;
