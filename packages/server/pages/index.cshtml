@page
@model IndexModel
@{
}

<div>
    <h1>Device settings</h1>
    @if (@Model.DeviceSettings != null){
    @foreach (var (deviceKey, deviceSetting) in @Model.DeviceSettings.Devices) {
        <h2>@deviceKey</h2>
        <p>Name: @deviceSetting.Name</p>
    }
    }
</div>