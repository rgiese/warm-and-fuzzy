@page
@model IndexModel
@{
}

<div>
    <h1>Device settings</h1>
    @foreach (var (deviceKey, deviceConfiguration) in @Model.DeviceConfigurations.Devices) {
        <h2>@deviceKey</h2>
        <p>Name: @deviceConfiguration.Name</p>
    }
</div>