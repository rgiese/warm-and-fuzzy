Resources:
  DeviceTenancyTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: DeviceTenancy
      KeySchema:
        # Partition key
        - AttributeName: deviceId
          KeyType: HASH
        # Sort key
        - AttributeName: tenant
          KeyType: RANGE
      AttributeDefinitions:
        # Partition key
        - AttributeName: deviceId
          AttributeType: S
        # Sort key
        - AttributeName: tenant
          AttributeType: S
      BillingMode: PAY_PER_REQUEST
